# Workplace Navigator Backend

FastAPI backend with PostGIS spatial queries and OpenRouter AI integration.

## Setup

```bash
python3 -m venv venv
source venv/bin/activate
pip install -e ".[dev]"
```

## Run

```bash
uvicorn app.main:app --reload
```
